% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_zensical.R
\name{install_zensical}
\alias{install_zensical}
\title{Install Zensical}
\usage{
install_zensical(path = ".", force = FALSE)
}
\arguments{
\item{path}{Path to the package root directory.}

\item{force}{Logical. If \code{TRUE}, recreates the virtual environment even if
it already exists.}
}
\value{
No return value, called for side effects.
}
\description{
Installs the Zensical static site generator into a Python virtual
environment using the \code{uv} package manager, as recommended by the Zensical
documentation. The virtual environment is created at \code{.venv_altdoc} (or at
the path specified by the \code{ALTDOC_VENV} environment variable) in the package
root directory.

This function is a convenience wrapper around the installation commands
documented at \url{https://zensical.org/docs/get-started/}. It is the
recommended way to install Zensical for use with \code{altdoc}.

If \code{uv} is not available on the system, the function falls back to using
\code{pip} in a standard Python virtual environment.
}
\examples{
if (interactive()) {
  install_zensical()
}
}
